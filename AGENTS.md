# Debugging Github Actions

- always use gh
- to debug, first download the run logs (not artifacts) into `test-output`, then grep

## Adding tests

`/host1` and `/host2` have nearly identical tests, always update both when making changes